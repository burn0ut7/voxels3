*** Begin Patch
*** Update File: Code/Voxels/VoxelManager.cs
@@
-				var nextBoundary = SelectClipboxBoundary( visualConfiguration, out _ );
+				var nextBoundary = SelectClipboxBoundary( visualConfiguration, out _, out var firstBoundary );
@@
-					prioritizePlayer = pendingBoundary > nextBoundary;
+					prioritizePlayer = pendingBoundary > firstBoundary;
@@
-	private int SelectClipboxBoundary( VoxelVisualConfiguration configuration, out Vector3Int boundaryAnchor )
+	private int SelectClipboxBoundary( VoxelVisualConfiguration configuration,
+		out Vector3Int boundaryAnchor, out int firstBoundaryLevel )
 	{
+		firstBoundaryLevel = -1;
@@
-			// Advance one nested boundary. Keep a coarse-region gap between seams.
+			var finest = _levels[configuration.MinimumVisualLod];
+			if ( !IsInsideHalfOpenBox( _targetLevelAnchors[finest.Level], finest.OuterMinimum, finest.OuterMaximum ) )
+			{
+				// Recover the final fine box and only the parent rings needed to contain it.
+				// Their final holes skip intermediate surfaces that finer terrain would replace.
+				for ( var level = finest.Level; level <= configuration.MaximumVisualLod; level++ )
+				{
+					var target = TargetOuterAnchor( level, configuration );
+					if ( level < configuration.MaximumVisualLod )
+					{
+						var extent = level == 0 ? configuration.Lod0VisualHalfExtent : configuration.LodCacheHalfExtent;
+						var minimum = _levels[level + 1].OuterMinimum * 2 + new Vector3Int( extent + 2 );
+						var maximum = _levels[level + 1].OuterMaximum * 2 - new Vector3Int( extent + 2 );
+						if ( target.x < minimum.x || target.y < minimum.y || target.z < minimum.z ||
+							target.x > maximum.x || target.y > maximum.y || target.z > maximum.z ) continue;
+					}
+					firstBoundaryLevel = finest.Level;
+					boundaryAnchor = target;
+					return level;
+				}
+			}
+			// Covered-player movement advances one boundary with the same seam gap.
@@
-		return boundaryLevel;
+		firstBoundaryLevel = boundaryLevel;
+		return boundaryLevel;
@@
-		var boundaryLevel = SelectClipboxBoundary( configuration, out var boundaryAnchor );
+		var boundaryLevel = SelectClipboxBoundary( configuration, out var boundaryAnchor, out var firstBoundaryLevel );
@@
-				? level == boundaryLevel ? boundaryAnchor : state.OuterAnchor
+				? level == boundaryLevel ? boundaryAnchor :
+					level >= firstBoundaryLevel && level < boundaryLevel ? TargetOuterAnchor( level, configuration ) : state.OuterAnchor
*** End Patch
